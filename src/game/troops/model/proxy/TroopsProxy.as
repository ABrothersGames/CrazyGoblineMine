/**
 * Created by HOME on 19.01.2017.
 */
package game.troops.model.proxy {
import org.puremvc.as3.patterns.proxy.Proxy;

public class TroopsProxy extends Proxy {
    public function TroopsProxy(proxyName:String = null, data:Object = null) {
        super(proxyName, data);
    }
}
}
